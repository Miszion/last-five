import './profile.css'

const Profile = (props) => {

    const {rank, icon, mostPlayed, winrate} = props

    return (
        <div className='hello'></div>
    )


}

export default Profile